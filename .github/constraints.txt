# CI-only, deliberately not in pyproject.toml so it does not reach users.
# simsopt 1.11.0's x86_64 wheel is built with AVX-512 and SIGILLs on the
# GitHub runners. Drop this once upstream ships a fixed release.
simsopt!=1.11.0
